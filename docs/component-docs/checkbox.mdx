# Checkbox

The Checkbox component is an atom in the AliveUI component library. It allows users to select one or multiple items from a set of options.

## Usage

```tsx
import { Checkbox } from "@aliveui/ui";

export default function CheckboxExample() {
  return (
    <div className="flex items-center space-x-2">
      <Checkbox id="terms" />
      <label
        htmlFor="terms"
        className="text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"
      >
        Accept terms and conditions
      </label>
    </div>
  );
}
```

## API Reference

The Checkbox component is built on top of the Radix UI Checkbox primitive.

### Properties

| Property          | Type       | Default | Description                                       |
| ----------------- | ---------- | ------- | ------------------------------------------------- |
| `checked`         | `boolean`  | -       | Whether the checkbox is checked                   |
| `defaultChecked`  | `boolean`  | -       | The default checked state when initially rendered |
| `onCheckedChange` | `function` | -       | Function called when the checked state changes    |
| `disabled`        | `boolean`  | `false` | Whether the checkbox is disabled                  |
| `required`        | `boolean`  | `false` | Whether the checkbox is required                  |
| `name`            | `string`   | -       | The name of the checkbox                          |
| `value`           | `string`   | -       | The value of the checkbox                         |

## Examples

### Basic Checkbox

```tsx
<div className="flex items-center space-x-2">
  <Checkbox id="terms" />
  <label
    htmlFor="terms"
    className="text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"
  >
    Accept terms and conditions
  </label>
</div>
```

### Disabled Checkbox

```tsx
<div className="flex items-center space-x-2">
  <Checkbox id="disabled" disabled />
  <label
    htmlFor="disabled"
    className="text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"
  >
    Disabled
  </label>
</div>
```

### Controlled Checkbox

```tsx
import { useState } from "react";

function ControlledCheckbox() {
  const [checked, setChecked] = useState(false);

  return (
    <div className="flex items-center space-x-2">
      <Checkbox
        id="controlled"
        checked={checked}
        onCheckedChange={setChecked}
      />
      <label htmlFor="controlled" className="text-sm font-medium leading-none">
        {checked ? "Checked" : "Unchecked"}
      </label>
    </div>
  );
}
```

### Checkbox Group

```tsx
<div className="grid gap-2">
  <div className="flex items-center space-x-2">
    <Checkbox id="option-1" />
    <label
      htmlFor="option-1"
      className="text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"
    >
      Option 1
    </label>
  </div>
  <div className="flex items-center space-x-2">
    <Checkbox id="option-2" />
    <label
      htmlFor="option-2"
      className="text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"
    >
      Option 2
    </label>
  </div>
  <div className="flex items-center space-x-2">
    <Checkbox id="option-3" />
    <label
      htmlFor="option-3"
      className="text-sm font-medium leading-none peer-disabled:cursor-not-allowed peer-disabled:opacity-70"
    >
      Option 3
    </label>
  </div>
</div>
```

## Accessibility

The Checkbox component follows accessibility best practices:

- Uses native HTML form controls when possible
- Supports keyboard navigation
- Associates labels using the `htmlFor` attribute
- Supports disabled states
- Includes proper ARIA attributes
- Maintains sufficient color contrast

## Implementation Details

The Checkbox component is built on top of Radix UI's Checkbox primitive, which provides a reliable and accessible foundation. The component is styled with Tailwind CSS, ensuring consistent appearance while allowing for customization through className props.
